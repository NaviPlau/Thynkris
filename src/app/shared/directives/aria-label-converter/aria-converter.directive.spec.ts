import { AriaConverterDirective } from './aria-converter.directive';

describe('AriaConverterDirective', () => {
  it('should create an instance', () => {
    const directive = new AriaConverterDirective();
    expect(directive).toBeTruthy();
  });
});
