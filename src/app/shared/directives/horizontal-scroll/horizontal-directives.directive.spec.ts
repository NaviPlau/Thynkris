import { HorizontalDirectivesDirective } from './horizontal-scroll/horizontal-directives.directive';

describe('HorizontalDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new HorizontalDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
